[tool.poetry]
name = "competence_core"
version = "0.1.0"
description = "Competence and skill map module"

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
faker = ">=37.4.2"

[build-system]
requires = ["poetry-core>=2.1.3"]
build-backend = "poetry.core.masonry.api"
